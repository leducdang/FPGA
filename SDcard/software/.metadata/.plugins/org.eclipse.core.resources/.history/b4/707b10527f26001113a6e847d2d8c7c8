///*
// * "Hello World" example.
// *
// * This example prints 'Hello from Nios II' to the STDOUT stream. It runs on
// * the Nios II 'standard', 'full_featured', 'fast', and 'low_cost' example
// * designs. It runs with or without the MicroC/OS-II RTOS and requires a STDOUT
// * device in your system's hardware.
// * The memory footprint of this hosted application is ~69 kbytes by default
// * using the standard reference design.
// *
// * For a reduced footprint version of this template, and an explanation of how
// * to reduce the memory footprint for a given application, see the
// * "small_hello_world" template.
// *
// */
//#include "sys/alt_stdio.h"
//#include "stdio.h"
//#include "system.h"
//#include "Altera_UP_SD_Card_Avalon_Interface.h"
//#include "unistd.h"
//
//
//int pixel;
//	int main(void)
//	{
//	    alt_up_sd_card_dev *device_reference = NULL;
//	    int connected = 0;
//
//	    device_reference = alt_up_sd_card_open_dev("/dev/SDCard");
//	    if (device_reference != NULL)
//	    {
//		printf("Initialized. Waiting for SD card...\n");
//		while(1)
//		{
//		    if ((connected == 0) && (alt_up_sd_card_is_Present()))
//		    {
//		        printf("Card connected.\n");
//		        if (alt_up_sd_card_is_FAT16())
//		        {
//			    printf("FAT16 file system detected.\n");
//
//			    printf("Looking for first file.\n");
//			    char * firstFile = "filenameunchanged";
//			    alt_up_sd_card_find_first(".", firstFile);
//			    printf("Volume Name: '%s'\n\n", firstFile);
//
//			    short file;
//			    while((file = alt_up_sd_card_find_next(firstFile)) != -1)
//			    {
//			        int contentCount = 0;
//			        printf("===========================\n");
//			        printf("Found file: '%s'\n", firstFile);
//
//			        short fileHandle = alt_up_sd_card_fopen(firstFile,false);
//			        printf("File handle: %i\n", fileHandle);
//
//			        printf("Contents:\n");
//			        short int readCharacter;
//			        short int readCharacter1;
//			        while ((readCharacter = alt_up_sd_card_read(fileHandle)) != -1)
//			        {
//			        	pixel = readCharacter;
//			        	readCharacter1 = alt_up_sd_card_read(fileHandle);
//			        	pixel = (pixel<<8)|readCharacter1;
//			            printf("%x\n", pixel);
//			            ++contentCount;
//
//			        }
//			        printf("\nContent size: %i", contentCount);
//			        printf("\n===========================\n\n");
//			    }
//		        }
//		        else
//		        {
//			    printf("Unknown file system.\n");
//		        }
//
//		        connected = 1;
//		    }
//		    else if ((connected == 1) && (alt_up_sd_card_is_Present() == false))
//		    {
//		        printf("Card disconnected.\n");
//		        connected = 0;
//		    }
//		}
//	    }
//	    else
//	    {
//		printf("Initialization failed.\n");
//	    }
//	}



/*
#include <stdio.h>
#include <altera_up_sd_card_avalon_interface.h>
int main(void) {

	printf("hello FPGA");
	alt_up_sd_card_dev *device_reference = NULL;
	int connected = 0;
	device_reference = alt_up_sd_card_open_dev("/dev/SDCard");
	if (device_reference != NULL) {
		while(1) {
			if ((connected == 0) && (alt_up_sd_card_is_Present())) {
				printf("Card connected.\n");
				if (alt_up_sd_card_is_FAT16()) {
					printf("FAT16 file system detected.\n");
				} else {
					printf("Unknown file system.\n");
				}
				connected = 1;
			} else if ((connected == 1) && (alt_up_sd_card_is_Present() == false)) {
				printf("Card disconnected.\n");
				connected = 0;
			}
		}
	}
	return 0;
}
*/


// ĐỌC TẤT CẢ KO THEO DÒNG
//#include <stdio.h>
//#include <stdbool.h>
//#include "altera_up_sd_card_avalon_interface.h"
////{
//    alt_up_sd_card_dev *device_reference = NULL;
//    int fileHandle;
//    int ch;
//
//    // Mở thiết bị SD Card
//    device_reference = alt_up_sd_card_open_dev("/dev/SDCard");
//    if (device_reference == NULL) {
//        printf("Khong mo duoc SD Card device\n");
//        return 1;
//    }
//
//    // Kiểm tra thẻ có cắm chưa
//    if (!alt_up_sd_card_is_Present()) {
//        printf("Chua cam the SD\n");
//        return 1;
//    }
//
//    // Kiểm tra định dạng FAT16
//    if (!alt_up_sd_card_is_FAT16()) {
//        printf("The khong phai FAT16\n");
//        return 1;
//    }
//
//    // Mở file TXT để đọc
//    fileHandle = alt_up_sd_card_fopen("TEST.TXT", false);
//    if (fileHandle < 0) {
//        printf("Khong mo duoc file TEST.TXT\n");
//        return 1;
//    }
//
//    printf("Noi dung file:\n");
//
//    // Đọc từng ký tự cho đến hết file
//    while ((ch = alt_up_sd_card_read(fileHandle)) >= 0) {
//        putchar((char)ch);
//    }
//
//    // Đóng file
//    alt_up_sd_card_fclose(fileHandle);
//
//    printf("\nDoc file xong\n");
//    return 0;
//}



//ĐỌC TỪNG DÒNG

//#include <stdio.h>
//#include <stdbool.h>
//#include "altera_up_sd_card_avalon_interface.h"
//
//int main(void)
//{
//    alt_up_sd_card_dev *device_reference = NULL;
//    int fileHandle;
//    int ch;
//    char line[128];
//    int idx = 0;
//
//    device_reference = alt_up_sd_card_open_dev("/dev/SDCard");
//    if (device_reference == NULL) {
//        printf("Khong mo duoc SD Card device\n");
//        return 1;
//    }
//
//    if (!alt_up_sd_card_is_Present() || !alt_up_sd_card_is_FAT16()) {
//        printf("SD Card loi hoac khong phai FAT16\n");
//        return 1;
//    }
//
////    fileHandle = alt_up_sd_card_fopen("TEST.TXT", false);
//    fileHandle = alt_up_sd_card_fopen("anhcaycoi.BMP", false);
//    if (fileHandle < 0) {
//        printf("Khong mo duoc file\n");
//        return 1;
//    }
//
//    while ((ch = alt_up_sd_card_read(fileHandle)) >= 0) {
//        if (ch == '\n' || idx >= sizeof(line) - 1) {
//            line[idx] = '\0';
//            printf("Dong vua doc: %s\n", line);
//            idx = 0;
//        } else if (ch != '\r') {
//            line[idx++] = (char)ch;
//        }
//    }
//
//    // In nốt dòng cuối nếu chưa có \n
//    if (idx > 0) {
//        line[idx] = '\0';
//        printf("Dong vua doc: %s\n", line);
//    }
//
//    alt_up_sd_card_fclose(fileHandle);
//    return 0;
//}


//
//#include <stdio.h>
//#include <stdbool.h>
//#include "altera_up_sd_card_avalon_interface.h"
//
//static int bmp_read_byte(short int fd)
//{
//    return alt_up_sd_card_read(fd);
//}
//
//static unsigned short bmp_read_u16_le(short int fd)
//{
//    int b0 = bmp_read_byte(fd);
//    int b1 = bmp_read_byte(fd);
//
//    if (b0 < 0 || b1 < 0) return 0xFFFF;
//    return (unsigned short)(b0 | (b1 << 8));
//}
//
//static unsigned int bmp_read_u32_le(short int fd)
//{
//    int b0 = bmp_read_byte(fd);
//    int b1 = bmp_read_byte(fd);
//    int b2 = bmp_read_byte(fd);
//    int b3 = bmp_read_byte(fd);
//
//    if (b0 < 0 || b1 < 0 || b2 < 0 || b3 < 0) return 0xFFFFFFFF;
//    return (unsigned int)(b0 | (b1 << 8) | (b2 << 16) | (b3 << 24));
//}
//
//int main(void)
//{
//    alt_up_sd_card_dev *sd_dev;
//    short int fd;
//
//    unsigned short bfType;
//    unsigned int   bfSize;
//    unsigned int   bfOffBits;
//
//    unsigned int   biSize;
//    int            biWidth;
//    int            biHeight;
//    unsigned short biPlanes;
//    unsigned short biBitCount;
//    unsigned int   biCompression;
//    unsigned int   biSizeImage;
//
//    int i;
//
//    sd_dev = alt_up_sd_card_open_dev("/dev/SDCard");
//    if (sd_dev == NULL) {
//        printf("Khong mo duoc SD device\n");
//        return 1;
//    }
//
//    if (!alt_up_sd_card_is_Present()) {
//        printf("Chua cam the SD\n");
//        return 1;
//    }
//
//    if (!alt_up_sd_card_is_FAT16()) {
//        printf("The khong phai FAT16\n");
//        return 1;
//    }
//
//    fd = alt_up_sd_card_fopen("anhcaycoi.bmp", false);
//    if (fd < 0) {
//        printf("Khong mo duoc file BMP\n");
//        return 1;
//    }
//
//    // ===== BITMAPFILEHEADER =====
//    bfType = bmp_read_u16_le(fd);      // 2 byte
//    bfSize = bmp_read_u32_le(fd);      // 4 byte
//    (void)bmp_read_u16_le(fd);         // reserved1
//    (void)bmp_read_u16_le(fd);         // reserved2
//    bfOffBits = bmp_read_u32_le(fd);   // offset toi pixel data
//
//    if (bfType != 0x4D42) { // 'BM'
//        printf("Day khong phai file BMP hop le\n");
//        alt_up_sd_card_fclose(fd);
//        return 1;
//    }
//
//    // ===== BITMAPINFOHEADER =====
//    biSize        = bmp_read_u32_le(fd);
//    biWidth       = (int)bmp_read_u32_le(fd);
//    biHeight      = (int)bmp_read_u32_le(fd);
//    biPlanes      = bmp_read_u16_le(fd);
//    biBitCount    = bmp_read_u16_le(fd);
//    biCompression = bmp_read_u32_le(fd);
//    biSizeImage   = bmp_read_u32_le(fd);
//
//    // Bo qua phan con lai cua BITMAPINFOHEADER (16 byte)
//    for (i = 0; i < 16; i++) {
//        if (bmp_read_byte(fd) < 0) {
//            printf("Loi khi doc header BMP\n");
//            alt_up_sd_card_fclose(fd);
//            return 1;
//        }
//    }
//
//    printf("BMP info:\n");
//    printf("File size    = %u bytes\n", bfSize);
//    printf("Data offset  = %u\n", bfOffBits);
//    printf("Width        = %d\n", biWidth);
//    printf("Height       = %d\n", biHeight);
//    printf("BitCount     = %u\n", biBitCount);
//    printf("Compression  = %u\n", biCompression);
//    printf("Image size   = %u\n", biSizeImage);
//
//    // Chỉ xử lý BMP không nén
//    if (biCompression != 0) {
//        printf("BMP dang nen, chua ho tro\n");
//        alt_up_sd_card_fclose(fd);
//        return 1;
//    }
//
//    // Nếu offset > 54 thì bỏ qua tới vùng pixel data
//    for (i = 54; i < (int)bfOffBits; i++) {
//        if (bmp_read_byte(fd) < 0) {
//            printf("Loi khi bo qua palette/header mo rong\n");
//            alt_up_sd_card_fclose(fd);
//            return 1;
//        }
//    }
//
//    // Ví dụ: đọc 16 byte đầu của pixel data
//    printf("16 byte dau cua pixel data:\n");
//    for (i = 0; i < 16; i++) {
//        int px = bmp_read_byte(fd);
//        if (px < 0) {
//            printf("\nHet du lieu som hoac loi doc\n");
//            break;
//        }
//        printf("%02X ", px & 0xFF);
//    }
//    printf("\n");
//
//    alt_up_sd_card_fclose(fd);
//    return 0;
//}
//
//
//
//
#include <stdio.h>
#include "altera_up_sd_card_avalon_interface.h"

int main()
{
    alt_up_sd_card_dev *sd_dev;
    short int fd;

    sd_dev = alt_up_sd_card_open_dev("/dev/SDCard");
    if (sd_dev == NULL) {
        printf("Khong mo duoc SD device\n");
        return 1;
    }

    if (!alt_up_sd_card_is_Present()) {
        printf("Khong co the SD\n");
        return 1;
    }

    if (!alt_up_sd_card_is_FAT16()) {
        printf("The khong phai FAT16\n");
        return 1;
    }

    fd = alt_up_sd_card_fopen("CAYCOI.BMP", false);
    if (fd < 0) {
        printf("Mo file BMP that bai, fd = %d\n", fd);
        return 1;
    }

    printf("Mo file BMP thanh cong\n");
    alt_up_sd_card_fclose(fd);
    return 0;
}

